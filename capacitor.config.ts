import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shopeasy.posapp',
  appName: 'Shopeasy',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
